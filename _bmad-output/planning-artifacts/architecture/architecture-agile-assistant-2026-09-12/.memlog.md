---
topic: Sprint Signal architecture spine
updated: 2026-09-12T00:54
---

- (decision by claude) Layered paradigm: UI -> API routes -> LLM layer -> JSON fixtures, single Next.js App Router project (spec.md section 6).
- (decision by claude) Only allowed external side effect is the Teams webhook POST; all other actions are local UI state (AD-3).
- (2026-09-14, product decision) Teams webhook removed entirely — AD-3 now reads "every write is local UI state," zero outbound network side effects. LLM provider was actually Gemini all along, not Anthropic Claude as originally specified here; corrected. Persistent storage (DB) explicitly planned for the next phase, not this one.
